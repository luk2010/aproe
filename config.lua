-- Configuration file to build atlanti's Project Engine
-- Change value to your own use !

-- Directory where will be setted every executables
appdir = path.getabsolute("bin")

-- Directory in the appdir where will go the plugins
plugindir = "plugins"

-- Directory in the appdir where will go the core library
libdir = ""

-- Directory in the appdir where will go the executables
execdir = ""

-- Directory to compile the object files
objectdir = path.getabsolute("obj")

-- Directory where the scripts are
scriptpath = path.getabsolute("premake4_scripts")
