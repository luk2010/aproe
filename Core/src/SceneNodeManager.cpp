/** @file SceneNodeManager.cpp
 *
 *  @author Luk2010
 *  @version 0.1A
 *
 *  @date 03/10/2012
 *
 *  @addtogroup Global
 *  @addtogroup Rendering
 *
 *  This file defines the SceneNodeManager class.
 *
**/
#include "SceneNodeManager.h"
/*
namespace APro
{
    SceneNodeManager::SceneNodeManager()
    {

    }

    SceneNodeManager::~SceneNodeManager()
    {

    }

    SceneNode::ptr SceneNodeManager::createEmptyNode(const String& name) const
    {
        return AProNew(1, SceneNode) (name);
    }

    RenderingContextSceneNode::ptr SceneNodeManager::createRenderingContextNode(const String& name, const Context::ptr& ctxt) const
    {
        return AProNew(1, RenderingContextSceneNode) (name, ctxt);
    }

    CameraSceneNode::ptr SceneNodeManager::createCameraNode(const String& name,
                                          const Vector3F& position,
                                          const Vector3F& direction) const
    {
        return AProNew(1, CameraSceneNode) (name, position, direction);
    }

    LightSceneNode::ptr SceneNodeManager::createLightNode(const String& name,
                                        const Color& ambient, const Color& diffuse, const Color& specular,
                                        const Vector3F& position, const Vector3F& direction,
                                        double exponent, double angle,
                                        float cst_att, float lin_att, float quad_att) const
    {
        return AProNew(1, LightSceneNode) (name, ambient, diffuse, specular,
                                           position, direction,
                                           exponent, angle,
                                           cst_att, lin_att, quad_att);
    }

    EntitySceneNode::ptr SceneNodeManager::createEntityNode(const String& name, const Entity::ptr& entity) const
    {
        return AProNew(1, EntitySceneNode) (name, entity);
    }
}
*/
